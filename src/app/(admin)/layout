// getLoginUser や isAdminUser は適当な関数が用意されているものとします。

// import { FC, ReactNode } from "react";

// const Layout: FC<{ children: ReactNode }> = asycn({ children })=> {
//     const user = await getLoginUser();
//     if(!isAdminUser(user)) {
//         return notFound();
//     }
//     return <div>{children}</div>;
// };

// export default Layout;
